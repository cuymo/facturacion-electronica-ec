{
  "name": "facturacion-electronica-ec-monorepo",
  "private": true,
  "description": "Libreria open source para emision de comprobantes electronicos del SRI Ecuador",
  "license": "MIT",
  "engines": {
    "node": ">=24.18.0"
  },
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm -r run typecheck",
    "lint": "pnpm -r --if-present run lint",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@types/node": "^24.13.2",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "xmllint-wasm": "^5.2.0"
  }
}
